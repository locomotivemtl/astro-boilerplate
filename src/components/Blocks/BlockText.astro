---
import Wysiwyg from '@components/Wysiwyg/Wysiwyg.astro';

interface Props {
    body?: string;
}

const { body } = Astro.props;
---

{body && (
    <Wysiwyg class="u-max-w-3xl u-mx-auto u-my-fluid-xl">
        <Fragment
            set:html={body}
        />
    </Wysiwyg>
)}
