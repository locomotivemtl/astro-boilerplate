---
interface Props {
    tag?: 'div' | 'article',
    body?: string;
    class?: string;
}

const {
    tag = 'div',
    body = null,
    class: propsClass,
    ...rest
} = Astro.props;

const Tag = tag || 'div'
---

{body &&
    <Tag
        class:list={['c-wysiwyg', propsClass]}
        set:html={body}
        {...rest}
    ></Tag>
}

<style is:global>
    @import 'Wysiwyg.scss';
</style>
