const config: Config = Object.freeze({
    // Utopia
    utopia: {
        minViewport: 320,
        maxViewport: 1440,
        rootSize: 16
    }
});

const CUSTOM_EVENTS: {} = Object.freeze({});

export { config, CUSTOM_EVENTS };
